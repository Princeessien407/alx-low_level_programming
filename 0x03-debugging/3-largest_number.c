#include <stdio.h>
#include "main.h"

/**
 * largest_number - return the largest of 3 numbers
 * @a: first interger
 * @b: second interger
 * @c: third interger
 * Return: largest number
 */

int largest_number(int a, int b, int c)
{
	int largest_number;

	printf("Enter first number: %d", a);
	scanf("%d", &a);
	printf("Enter second number: %d", b);
	scanf("%d", &b);
	printf("Enter second number: %d", c);
	scanf("%d", &c);

	if (a > b && b > c)
	return (largest);
}
